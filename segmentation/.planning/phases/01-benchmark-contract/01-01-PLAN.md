# Phase 1 Plan: Benchmark Contract & Data Split

## Goal
Define the benchmark contract, fixed data split, and reproducibility rules before any training starts.

## Requirements Covered
DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, DATA-06, REPRO-01, REPRO-04, REPRO-05

## Tasks

### 1. Create `configs/dataset.yaml`
- Dataset: ISIC 2018 Task 1
- Root path: `/home/furkan/Projects/nnU-net/archive`
- Modality: Dermoscopy (2D RGB)
- Labels: Binary (0=background, 1=lesion)
- Variable image sizes → models resize as needed

### 2. Create `splits/primary_split.json`
- Patient-level split from 2594 training images (only set with GT masks)
- 70% train / 15% val / 15% test (~1816 / 389 / 389)
- ISIC IDs serve as patient identifiers (one image per patient)
- Deterministic: sorted IDs, fixed seed split

### 3. Create `configs/seeds.yaml`
- Seeds: [11, 22, 33]

### 4. Create `configs/metrics.yaml`
- Required: Dice, IoU, HD95, latency, peak GPU memory

### 5. Create `docs/benchmark_protocol.md`
- Fairness rules, MedSAM prompt protocol, execution policy

## Verification
- Zero overlap test on split
- All files exist and are valid
